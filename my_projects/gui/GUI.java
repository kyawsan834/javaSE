import javax.swing.JOptionPane;
import java.util.Random;
import java.util.Scanner;

public class GUI{

	public static void main(String [] args){

		// System.out.println("hay");

		// String name = JOptionPane.showInputDialog("Enter your name...");
		// JOptionPane.showMessageDialog(null, "Hello " + name);
		// System.out.println("Your name is " + name);

		// int age = Integer.parseInt(JOptionPane.showInputDialog("Enter you age"));
		// if(age >= 1 && age<=150){
		// 	JOptionPane.showMessageDialog(null,"Hello "+ name + " your are " + age + " years old.");
		// 	System.out.println("Your age is " + age);
		// }else{
		// 	JOptionPane.showMessageDialog(null, "Invalid age");
		// 	System.out.println("Invalid age...");
		// }

		// double height = Double.parseDouble(JOptionPane.showInputDialog("Enter your height"));
		// JOptionPane.showMessageDialog(null,"Your height is " + height + " cm talls.");
		// System.out.println("Your height is " + height);

		double x = 3.14;
		double y = -10;Collection

		double z;
		z = Math.max(x,y);
		z = Math.min(x,y);
		z = Math.abs(x);
		z = Math.abs(y);
		z = Math.sqrt(x);
		z = Math.sqrt(y); // NaN
		y = 10;
		z = Math.sqrt(y);
		z = Math.round(x);
		z = Math.round(y);
		z = Math.round(x);
		z = Math.round(y);
		z = Math.floor(x);
		z = Math.floor(y);

		// System.out.println(z);

		// Assignment for Right Angel Triangle(hypotenuse) calculate with Math Class

		double a,b,getSize;

		Scanner sc = new Scanner(System.in);


		/*System.out.println("Enter side a:");
		a = sc.nextDouble();

		System.out.println("Enter side b:");
		b = sc.nextDouble();

					// formula = square root (x*x)+(y*y)
		getSize = Math.sqrt((a*a)+(b*b));

		System.out.println("The hypotenuse is : " + getSize);

		sc.close();*/


		// Random Class

		/*Random random = new Random();

		int num1 = random.nextInt(6)+1;
		System.out.println(num1);

		double num2 = random.nextDouble();
		System.out.println(num2);

		boolean num3 = random.nextBoolean();
		System.out.println(num3);*/




		

	

	}
	
}